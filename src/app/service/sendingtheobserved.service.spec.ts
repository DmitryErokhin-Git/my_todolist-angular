/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { SendingtheobservedService } from './sendingtheobserved.service';

describe('Service: Sendingtheobserved', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [SendingtheobservedService]
    });
  });

  it('should ...', inject([SendingtheobservedService], (service: SendingtheobservedService) => {
    expect(service).toBeTruthy();
  }));
});
